package com.example.truyvetyte

object Constants {
    // Đây là URL gốc của server
    const val BASE_URL = "https://api-yte-backend.onrender.com"

    // Bạn có thể nối sẵn các endpoint cụ thể ở đây luôn cho tiện
    const val REGISTER_URL = "$BASE_URL/api/register"
    const val LOGIN_URL = "$BASE_URL/api/login"
}